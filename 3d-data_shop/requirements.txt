# ── 必須（Python 3.14 標準ライブラリに含まれない）──────────────────────────
matplotlib>=3.7.0
Pillow>=9.0.0

# ── CadQuery 環境（Python 3.9-3.11 推奨）───────────────────────────────────
# cadquery>=2.3.0   ← Python 3.10 環境でのみインストール可
# 自動検出: setup.bat / setup.sh が Python 3.10 venv を作成

# ── オプション（品質向上）─────────────────────────────────────────────────
# trimesh>=3.22.0   ← STL 品質チェック用
# open3d>=0.17.0    ← 高品質サムネイル用
